'use client'
import type { MakeInitial } from './MakeAppointmentTab'

// Placeholder — full implementation is a separate task.
export default function RecommendTab({ initial: _initial }: { initial: MakeInitial }) {
  return (
    <div className="flex flex-col gap-3 p-4">
      <p className="text-xs text-text-secondary">Recommendations coming soon.</p>
    </div>
  )
}
